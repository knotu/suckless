# supposed to copy text
"sleep 0.2; xdotool key Control_L+c"
b:10

# supposed to paste text
"sleep 0.2; xdotool key Control_L+v"
b:11

# This copies text(barcode), opens a new tab and submits google search. Then it opens a new tab and copies text(barcode) to custom upc search and submits"
"xdotool key Control_L+c Control_L+t Control_L+v Return Control_L+t u space Control_L+v --delay 40 Return"
  Control + b:10
  
# This copies text(barcode), opens a new tab and submits google search. Then it opens a new tab and copies text(barcode) to custom upc search and submits"
#"xdotool key Control_L+c Control_L+t Control_L+v Return Control_L+t u space Control_L+v --delay 40 Return"
#  Control + b:9  

# closes tab in Chromium
# this is originally b:8. phantom clicks on mouse made me switch
"sleep 0.1; $HOME/bin/app-key-chromium.sh"
  b:9
   
# switch tabs in Chromium
#"sleep 0.1; $HOME/bin/tab-switch-browser.sh"
#  b:9  
   
   
